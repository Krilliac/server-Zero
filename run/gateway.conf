################################################################################
# Cluster gateway daemon configuration                                         #
################################################################################

[GatewayConf]
ConfVersion=2026060300

################################################################################
# GATEWAY SETTINGS
#    Format: hostname;port;username;password;database
################################################################################
LoginDatabaseInfo      = "127.0.0.1;3306;root;root;realmd"
CharacterDatabaseInfo  = "127.0.0.1;3306;root;root;character0"

GatewayPort            = 8085

# Number of backend nodes behind this gateway. Each Node.<n>.IntakePort below must
# match that node's Gateway.IntakePort (in its mangosd.conf), and every node must
# share this gateway's Gateway.Secret. Set to 1 for a single node; raise it + add
# Node.2/Node.3... to add more nodes.
Node.Count             = 2

Node.1.Host            = 127.0.0.1
Node.1.IntakePort      = 9100

# Second node (only used when Node.Count >= 2):
Node.2.Host            = 127.0.0.1
Node.2.IntakePort      = 9101

Gateway.Secret         = testsecret123

LogFile                = "Gateway.log"
PidFile                = ""
LogLevel               = 3

# Phase 1 anti-cheat channel self-test (diagnostic). 1 = send one benign
# GW_AC_EVENT per client at session open so the gateway->node->AntiCheatMgr
# round-trip shows up in the node log. Leave 0 in normal use.
Gateway.SelfTestAcEvent = 0
